<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  <mapper namespace="com.goodjob.blacklist">
  	<select id="manBlackListGet" parameterType="Map" resultType="com.goodjob.member.model.MemberDTO">
		select * from
		(select rownum as rnum, a.* from(
		select * from member where status in('활성','블랙') and user_category=#{category} and singo_count<![CDATA[>=]]>20 order by idx desc)a )b
		where rnum <![CDATA[>=]]>#{start} and rnum <![CDATA[<=]]>#{end}
	</select>
	<select id="manBlackListGetTotalCnt" parameterType="String" resultType="Integer">
		select
			count(*)
		from
			member
		where
			status in('활성','블랙')
			and
			user_category=#{category}
	</select>
  </mapper>